# Supabase (already used by amenities)
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_key

DATABASE_URL=postgresql://postgres.lbgsnpwlycvbdvkpcgxp:0mqCy04JW5lV6FBl@aws-0-us-west-1.pooler.supabase.com:6543/postgres


# Casafari auth (choose one)
# Preferred: full Authorization header value
CASAFARI_AUTH=Bearer YOUR_CASAFARI_TOKEN
# Or fallback: token only, defaults to Bearer
# CASAFARI_API_TOKEN=YOUR_CASAFARI_TOKEN

# Optional: override base URL (default: https://api.casafari.com)
# CASAFARI_API_BASE_URL=https://api.casafari.com

# CRUS zoning via OGC API - Features only (GeoJSON + CQL2)
# Discover collections: https://ogcapi.dgterritorio.gov.pt/collections
# Note: DGT currently exposes multiple CRUS collections (often municipal). Pick the one that covers your plots.
# Example configuration:
# DGT_CRUS_OAPI_BASE=https://ogcapi.dgterritorio.gov.pt
# DGT_CRUS_OAPI_COLLECTION=crus_lousa
# Optional: geometry property name on OGC API (defaults to 'geometry')
# DGT_CRUS_OAPI_GEOM_PROP=geometry

# DGT_CRUS_SRS=EPSG:4326
# DGT_CRUS_LABEL_FIELDS=classe,classe_solo,qualificacao,uso,uso_solo,categoria,classe1,desc_class
CRUS_INTER_PLOT_DELAY_MS=500

CRUS_CONCURRENCY=5

# Directory where Casafari search JSON pages are saved (default: ./outputs/casafari-search)
CASAFARI_OUTPUT_DIR=./outputs/casafari-search

# Gemini / LLM enrichment
# Obtain an API key from Google AI Studio / Google Cloud Vertex AI (depending on SDK and account).
# Required: Google API key
GOOGLE_API_KEY=your_google_api_key
# Optional: model name (default: gemini-1.5-pro). If you have access to a newer model, set it here.
GEMINI_MODEL=gemini-1.5-pro
# Enable Search Tool grounding (uses Google Search; may require specific access/plan)
# Batch size and concurrency for LLM enrichment
LLM_REALTORS_LIMIT=200
LLM_REALTORS_CONCURRENCY=2

# Default country code for realtor extraction and backfill (ISO-2 or ISO-3; used when country is unknown)
REALTORS_DEFAULT_COUNTRY=PT

# CRUS LLM-powered translation
# Enable to translate zoning label to English using Gemini
# CRUS_TRANSLATE=true
# Optional target language (default: en)
# CRUS_TRANSLATE_TARGET_LANG=en
# Optional source language hint (default: pt)
# CRUS_TRANSLATE_SOURCE_LANG=pt
# Optional: override model used specifically for translation (falls back to GEMINI_MODEL)
# CRUS_TRANSLATE_MODEL=gemini-1.5-pro