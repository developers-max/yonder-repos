# Yonder Agent Authentication Configuration Examples

# ============================================
# MODE 1: Google Cloud Auth (Auto-Refresh) - RECOMMENDED
# ============================================
# Automatically refreshes tokens before expiration
# Best for production and Google Cloud deployments

USE_GCLOUD_AUTH=true
YONDER_AGENT_API_URL=https://your-agent-api.example.com

# ---- Option A: Service Account JSON (Recommended for Production) ----
# Store the entire JSON content as a string in environment variable
# Perfect for secret managers (AWS Secrets Manager, Azure Key Vault, etc.)
GOOGLE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"yonder-477414","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n","client_email":"yonder-agent-client@yonder-477414.iam.gserviceaccount.com",...}'

# ---- Option B: Service Account Key File ----
# Use a file path instead (less secure for production)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json

# ---- Option C: Application Default Credentials (ADC) ----
# Use your personal Google account for local development
# Run: gcloud auth application-default login
# Then just set:
# USE_GCLOUD_AUTH=true
# YONDER_AGENT_API_URL=https://your-agent-api.example.com


# ============================================
# MODE 2: Manual Bearer Token (No Auto-Refresh)
# ============================================
# Uses static token from environment
# Best for development/testing or non-GCloud environments
# WARNING: Token will expire and must be manually refreshed

# USE_GCLOUD_AUTH=false  # or leave unset
GCLOUD_TOKEN=ya29.c.b0AXv0zTPj...your-bearer-token-here
YONDER_AGENT_API_URL=http://localhost:8080


# ============================================
# TOKEN GENERATION EXAMPLES
# ============================================

# Generate a short-lived token for testing:
# gcloud auth print-access-token

# Generate a token with specific scopes:
# gcloud auth application-default print-access-token

# Create a service account and download key:
# gcloud iam service-accounts create yonder-agent-client
# gcloud iam service-accounts keys create key.json \
#   --iam-account=yonder-agent-client@PROJECT_ID.iam.gserviceaccount.com
